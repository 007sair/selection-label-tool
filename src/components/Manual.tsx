export default function Manual() {
  return (
    <div style={{ padding: "10px 0 10px 15px", width: 500 }}>
      <ol>
        <li>在左侧“标签配置”区填入配置代码；</li>
        <li>“标签展示”区会实时显示代码对应的树；</li>
        <li>对树的节点进行编辑、拖拽重新排序；</li>
        <li>点击右上角的“预览”按钮后，复制更改后的代码配置。</li>
      </ol>
    </div>
  );
}
